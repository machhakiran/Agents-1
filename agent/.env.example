# Copy to .env and set values. See README_CREDENTIALS.md for MANDATORY vs default.
# Do not commit .env.

# ----- Optional: App -----
# APP_NAME=ai-dev-agent
# DEBUG=false
# LOG_LEVEL=INFO
# HOST=0.0.0.0
# PORT=8000

# ----- Optional: Idempotency -----
# IDEMPOTENCY_BACKEND=memory
# REDIS_URL=redis://localhost:6379/0

# ----- Optional: Git defaults -----
# GIT_PROVIDER=github
# DEFAULT_BRANCH=main

# ----- MANDATORY for private repos / PR creation -----
# GitHub: Personal Access Token with repo scope (no default)
GITHUB_TOKEN=

# GitLab: Access token (no default when using GitLab)
# GITLAB_TOKEN=
# GITLAB_URL=https://gitlab.com

# ----- Optional: Git clone (HTTPS auth if not using token in URL) -----
# GIT_USERNAME=
# GIT_PASSWORD=

# ----- MANDATORY for Phase 2+ (code generation) -----
# Anthropic API key for Claude (required for map → plan → implement)
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ----- Optional: Pipeline -----
# WORKSPACE_BASE=/tmp/ai_agent_workspaces
# MAX_VALIDATION_RETRIES=5
# TASK_TIMEOUT_SECONDS=1800
# PR_LABEL_AI_GENERATED=ai-generated

# ----- Optional: Jira status sync -----
# JIRA_BASE_URL=
# JIRA_USERNAME=
# JIRA_API_TOKEN=
